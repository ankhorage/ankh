---
'@ankhorage/ankh': patch
---

Replace the `ankh commands` listing with conventional root and package help, using installation-scoped provider discovery so help no longer depends on the current working directory.
